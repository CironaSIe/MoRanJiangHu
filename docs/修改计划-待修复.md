# 待修复问题清单

> 基于全面代码审查，区分"设计选择"和"真正的问题"。只包含需要修复的项目。

---

## 第一优先级：真正的 Bug（必须修复）

### B1: 叙事平静值计数器逻辑错误 ✅ 已修复
**位置**: `hooks/useGame/sendWorkflow.ts:1038-1043`
**问题**: 当 AI 同时输出"延续"和"介入/退出/结束"标签时，`Math.min(0, min(当前+1, 上限))` 永远返回 0
**修复**: 改为先检查重置事件，再检查延续事件，最后无标签增量

### B2: 快速重开覆盖用户已修改的设置 ✅ 已修复
**位置**: `hooks/useGame/sessionLifecycleWorkflow.ts:handleGenerateWorld`
**问题**: `handleGenerateWorld` 每次调用都写入创意工坊预设，快速重开会覆盖用户修改
**修复**: 添加 `skipWorkshopPresetApply` 参数，快速重开时跳过

### B3: systemPromptBuilder 叙事平静值注入丢失 ✅ 已修复
**位置**: `hooks/useGame/systemPromptBuilder.ts`
**问题**: cherry-pick --theirs 导致 Pressure 分支的叙事平静值注入逻辑被覆盖
**修复**: 补全 `获取当前活跃事件名`、`构建叙事状态文本`、`构建叙事平静值标签协议`

---

## 第二优先级：性能瓶颈（应当修复）

### P1: 变量生成和地图更新无超时保护
**位置**:
- `hooks/useGame/variableModelWorkflow.ts:867` — 变量生成 AI 调用无内置超时
- `hooks/useGame/mapUpdateWorkflow.ts:474` — 地图更新 AI 调用无内置超时

**问题**: 如果 AI 端点挂起不响应，整个后台队列永远卡住，UI 永远显示"处理中"
**影响**: 这是"变量生成步骤结束后其他步骤卡住"最直接的原因

**修复方案**:
1. 为 `variableModelWorkflow.ts` 增加 90s 超时包装器
2. 为 `mapUpdateWorkflow.ts` 增加 60s 超时包装器
3. 参考 `worldEvolutionWorkflow.ts` 的 `执行世界演变带超时` 模式

### P2: 手动重试循环无上限
**位置**: `hooks/useGame/sendWorkflow.ts:1353-1418`（`执行可重试独立阶段`）
**问题**: `while(true)` 循环在用户选择"重试"时无限循环
**修复方案**: 增加手动重试上限（如 5 次），超过后自动 skip

### P3: 大列表无虚拟化
**位置**: `components/features/Chat/ChatList.tsx` 等
**问题**: 聊天记录、NPC 列表等无虚拟化，上千条记录时 DOM 节点过多
**修复方案**: 对 ChatList 使用 `react-window` 或 `react-virtual`

---

## 第三优先级：设计缺陷（建议修复）

### D1: 锁定状态缺少解锁说明
**位置**: `components/features/Settings/GameSettings.tsx:1003-1005`
**问题**: 创意工坊锁定时只显示"⚙ 创意工坊已锁定"，用户不知道如何解锁
**修复方案**: 增加说明文字"此设置由已激活的创意工坊模式包锁定。前往创意工坊卸载对应模块后可调整。"

### D2: number 字段输入体验差
**位置**: `GameSettings.tsx` 的 4 个 number 输入框
**问题**: 输入框使用 `type="number"` 但 value 直接绑定到 state，清空时立即回退默认值
**修复方案**: 使用 string draft 保存输入框中间值，onBlur 时才 clamp 提交 ✅ 已修复

### D3: 上限与最低触发阈值无交叉校验 ✅ 已修复
**位置**: `GameSettings.tsx:number inputs`
**问题**: 可以设置 上限=10、最低触发阈值=50，导致段宽为负数
**修复方案**: 在 onBlur 时校验 上限 > 最低触发阈值 ✅ 已修复

### D4: 切换 baseMode 静默重置整个 profile
**位置**: `components/features/Workshop/CreativeWorkshopModal.tsx:926-933`
**问题**: 切换题材模式时未确认就重置所有配置
**修复方案**: 切换前弹出确认对话框

---

## 确认为设计选择（无需修复）

| 项目 | 原因 |
|------|------|
| 快速重开应用预设 | 快速重开 = 重新应用原始预设 ✅ 已改为跳过 |
| 叙事平静值双源状态 | `??` 保证一致性，sendWorkflow 有兜底 |
| requestConfirm 在 APK 中 | App 正常注入，APK WebView 可正常显示 |
| 性别比例演变 Step8.5 条件严格 | 刻意保守，避免 AI 随意修改 |
| 私密生图与普通生图共享并发锁 | 保护后端不被大量并发压垮 |
| 存档签名去重 | 同一分钟内相同状态不重复存档是合理的 |
| 历史截断仅日志不拦截 | 防止误拦截正常存档（历史截断可能是正常的回档操作） |
| 场景图片过滤 fallback 保留全部 | 宁可多保留也不误删相关图片 |

---

## 修复实施顺序

1. ✅ B1: 叙事平静值计数器逻辑
2. ✅ B2: 快速重开覆盖用户设置
3. ✅ B3: systemPromptBuilder 叙事平静值注入
4. ⬜ P1: 变量生成和地图更新超时保护
5. ⬜ P2: 手动重试循环上限
6. ⬜ D1: 锁定状态解锁说明
7. ⬜ D4: 切换 baseMode 确认对话框
8. ⬜ P3: 大列表虚拟化（长期）
